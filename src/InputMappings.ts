const moveLeft = "A";
const moveRight = "D";
const moveUp = "W";
const moveDown = "S";
const toggleDebug = "P";

class InputMappings {
    static MOVE_LEFT() {
        return moveLeft;
    }

    static MOVE_RIGHT() {
        return moveRight;
    }

    static MOVE_UP() {
        return moveUp;
    }

    static MOVE_DOWN() {
        return moveDown;
    }

    static TOGGLE_DEBUG() {
        return toggleDebug;
    }
}